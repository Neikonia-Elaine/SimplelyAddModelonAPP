ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
}
jwt {
    audience = "my-audience"
    issuer = "http://localhost/"
    realm = "My realm"
    secret = "TESTING_SECRET_XYZABCDEFG"
}